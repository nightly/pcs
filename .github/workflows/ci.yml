name: ci 

on: 
  push:
    branches: [master] 
  pull_request:
    branches: [master]

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [windows-latest]

    steps:
    - uses: actions/checkout@v2
      with:
        submodules: recursive

    - name: Configure
      run: |
        cmake -S. -B build/x64/release -DCMAKE_BUILD_TYPE=x64-Release
    - name: Build
      run: |
        cmake --build build/x64/release
    - name: Run Tests 
      run: |
        cd build/x64/release
        ctest -j 8 --output-on-failure --verbose